// Placeholder for LeafletZoneSelector component (to be implemented in Issue #4)
import { forwardRef } from 'react';

export interface LeafletZoneSelectorProps {
  // To be defined in Issue #4
}

export interface LeafletZoneSelectorRef {
  // To be defined in Issue #4
}

export const LeafletZoneSelector = forwardRef<LeafletZoneSelectorRef, LeafletZoneSelectorProps>(
  (_props, _ref) => {
    return <div>LeafletZoneSelector placeholder</div>;
  }
);

LeafletZoneSelector.displayName = 'LeafletZoneSelector';